"""Compatibility placeholder.

The live `models` import now resolves to the package in `models/`, which
contains the split Pydantic schemas. This file remains only to make the
module/package transition explicit in the repository layout.
"""
